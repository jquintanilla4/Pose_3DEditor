{
  "name": "pose-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "dev:frontend": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "pyserver": "uvicorn server.main:app --reload --host 0.0.0.0 --port 8000"
  },
  "devDependencies": {
    "@types/three": "^0.181.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.2"
  },
  "dependencies": {
    "three": "^0.181.1"
  }
}
